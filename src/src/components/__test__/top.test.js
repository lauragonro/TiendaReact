import React from 'react';
import { render, screen } from '@testing-library/react';
import Head from '../top'; // Importa el componente que deseas probar

test('Renderiza Head correctamente', () => {
  render(Head); // Renderiza el componente


});
